# 클라우드 GPU(CUDA) 학습 환경. 맥 아님. Runpod/Colab Linux+NVIDIA에서 설치.
# torch는 GPU 이미지(RunPod PyTorch)에 CUDA판이 기설치됨 → 여기서 재설치 금지(덮어쓰면 CPU판 위험).
#   (torch 없는 환경이면 별도로 CUDA 빌드 설치)
transformers>=4.46
datasets
accelerate
peft>=0.13
trl>=0.12
bitsandbytes>=0.44
