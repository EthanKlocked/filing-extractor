# env / secrets
.venv/
.env
*.key

# python
__pycache__/
*.pyc
.ipynb_checkpoints/

# data & model artifacts (대용량 / 일부 비공개)
data/raw/
data/private/
*.safetensors
*.gguf
checkpoints/
wandb/
# 단, 학습된 LoRA 어댑터는 레포에 포함(디바이스 간 포터블 — 57MB)
!03-finetune-qlora/outputs/adapter/adapter_model.safetensors

# os
.DS_Store

# 04 serving 파생물 (대용량, 재생성 가능)
04-serving/merged/
