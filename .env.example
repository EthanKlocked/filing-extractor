# 이 파일을 복사해 .env 로 만들고 값을 채우세요. (.env 는 .gitignore 됨 — 커밋 안 됨)
# cp .env.example .env

# distillation teacher 모델 (라벨 생성용). Gemini API 키.
GEMINI_API_KEY=

# (선택) SEC EDGAR 요청 시 User-Agent 에 쓸 연락처
SEC_EDGAR_EMAIL=
